# Ticketing Admin Control Center — v1.3 Implementation Tasks

> Use "Page context/ticketing-admin-spec.md" as the source of truth for detailed field lists, copy, and behavior — this checklist is the execution order. Don't add any conditional visibility, permission logic, or styling beyond what's described in the spec. If something's ambiguous, ask instead of assuming. Work through tasks in order — Task 2 (route shell) must exist before Tasks 3–8 since they all render inside it. After finishing each task, check it off below and briefly summarize what changed before moving to the next task.

- [x] **1. User account dropdown menu**
  - On the main Ticketing page, add a chevron icon beside the user account name to indicate it opens a dropdown.
  - Dropdown items: "My Profile" (non-functional, just present), "Admin Control Center" (functional, navigates to the new admin route), "Sign Out" (non-functional, just present). Do NOT add a "My Tickets" item.

- [x] **2. Admin Control Center route shell**
  - Create a new route (e.g. `/ticketing/admin` as base) — separate from the existing `/admin` route used by the unrelated data source connections admin feature. Don't reuse or collide with that route's naming.
  - Layout: left sidebar with 6 sections — Dashboard, Executive Summary, Audit Log, Manage Users, Content Moderation, Manage Categories. Active section is visually highlighted.
  - Top bar includes a "Back to Forum" button, positioned left of the account profile/icons, which navigates back to the main ticketing page.
  - Each sidebar section can route to its own sub-path (e.g. `/ticketing/admin/dashboard`, `/ticketing/admin/executive-summary`, etc.) — implement whichever routing structure fits the existing router setup best.

- [x] **3. Dashboard section**
  - "System Overview" header with a short SLA compliance subtitle line.
  - 4 summary cards: Total Tickets, Open & In Progress, Closed (Archive), SLA Breach — mock numbers, reasonably consistent with existing ticketing mock data counts.

- [x] **4. Executive Summary section**
  - 4 summary cards: Open Tickets, Closed Tickets, SLA Meet Rate (%), Avg Aging (Open) (time duration).
  - "Daily Activity — Last 7 Days" table: columns Date, Opened, Closed, Trend. Trend column renders as a mini horizontal stacked bar (2 colors matching the Opened/Closed legend below the table) — not a sparkline or line chart.
  - 7 rows of mock daily data.
  - "Today's Activity" card below the table, summarizing today's opened/closed counts in one line.

- [x] **5. Audit Log section**
  - Header: "Activity Tracking & Form Audit Log (SQLite DB)" with a small "ndq_forum.db" tag and a "Total: N Log" counter.
  - Table columns: Timestamp, Action Type, User, Form Activity Details, IP Address.
  - Ship with zero mock rows and the empty state text: "No activity logs found in database."

- [x] **6. Manage Users section**
  - Header: "User Management" with subtitle and a "Total Users: N" counter.
  - Search input ("Search by name, @username, or email...") and a role filter dropdown ("All Roles").
  - Table columns: User, Email, Department, Role, Status, Timestamps, Action.
  - Populate with mock data reusing existing mock people already used elsewhere in ticketing (Fengky Pratama, Ivan Nurcahyo, etc.) rather than inventing new names.

- [x] **7. Content Moderation section**
  - Header: "Content Moderation" with subtitle, plus filter tabs: All (N), Pinned, Solved.
  - Reuse the existing ticket card component already built for the main NDQ-Forum board/list — don't rebuild from scratch. Make it responsive to fit this admin list layout.
  - Add action items on the right side of each card: Pin/Unpin (toggle), Mark Solved/Unmark Solved (toggle), Delete.
  - These actions are functional against local mock state (in-memory React state): Delete removes the ticket from the list, Pin/Unpin affects the "Pinned" tab filter, Mark Solved/Unmark Solved affects the "Solved" tab filter. No persistence beyond the current session — a page refresh resets to original mock data, which is expected.

- [x] **8. Manage Categories section**
  - Header: "Manage Forum Categories" with subtitle about add/edit/remove + audit trail note.
  - Two tabs: Categories / Requests.
  - A "+ Create Root Category" button (can be non-functional for now, just present).
  - Ship with blank/empty category data and the empty state text: "No categories found. Create a new category above to get started."
